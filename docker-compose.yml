version: '3.8'

services:
  epubtrans:
    build: .
    network_mode: host
    volumes:
      - ./uploads:/app/uploads
      - ./unpackage:/app/unpackage
    environment:
      - OPENAPI_URL=http://localhost:8000/v1/chat/completions
    command: ['./epubtrans', 'serve', '/app/uploads', '--port', '3000']
    restart: unless-stopped
